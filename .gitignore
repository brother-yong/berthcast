# Data files — never upload these
Customer Listing/
Inventory Report/
Purchase Order Record/
Sales Report/
Stockout Report/
Supplier Listing/
uploads/

# Client exports & personal working folders — real client financials live here,
# never upload (the *.pdf rule below doesn't cover the .xlsx/.csv inside them)
exports for yh/
outreach/
landing_preview.html

# Credentials & captured sessions — never upload
cookies*.txt
*.cookies

# Local tooling — Claude worktrees, plans, local settings (not for the repo)
.claude/

# Local memory / working notes & AI instructions — contain client financials and
# personal info, never upload
MEMORY.md
CLAUDE.md

# Stray client exports / design scratch that sometimes land here — never upload
fixtures/berthcast_orders_*.csv
design-preview/
.superpowers/

# Database
*.db
*.db-journal
*.db-shm
*.db-wal

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# Generated reports and output files
*.pdf
*.docx

# OS
.DS_Store
Thumbs.db
desktop.ini
